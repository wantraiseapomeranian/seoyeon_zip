# 서연모음.zip — Design Specification

작성일: 2026-09-09
저장소명: `seoyeon-zip`
상태: 구현 인계용 디자인 기준. 사용자 선택, 제안 기본값, 시안에서 결정할 사항을 구분한다.

## 1. 목적과 문서 우선순위

서연모음.zip은 tripleS 윤서연의 사진과 영상을 여러 출처에서 모아보는 개인용 미디어 아카이브다. 사용자는 사진을 훑어보고 관심 있는 게시물의 원문으로 이동한다.

이 문서는 시각적 방향과 화면 동작을 정의한다. 수집·데이터·배포 계약은 `SEOYEON_MEDIA_V1_SPEC.md`를 따른다. 기존 설계서의 가제 “서연 모아보기”는 “서연모음.zip”으로, 임시 보라색 포인트는 이 문서의 하늘색 방향으로 대체한다. 기능 범위를 확대하는 근거로 디자인 레퍼런스를 사용하지 않는다.

사용자의 이후 지시가 두 문서보다 우선한다. 미확정된 선택을 사용자 승인 사실로 보고하지 않는다.

## 2. 결정 상태

아이콘 선택: 사용자는 3종 비교에서 첫 번째 Phosphor Regular를 선택했다. 목록 새로고침은 arrows-clockwise, 수집 상태는 list-checks를 사용한다. 아이콘 22px, 버튼 44px, 테두리 없는 기본 상태와 키보드 포커스·기능 이름 툴팁을 유지한다. 공식 SVG를 사용하며 React 의존성을 추가하지 않는다.

2026-09-09 최종 시안 선택: 사용자는 A의 여유 있는 3열 배치를 바탕으로 작은 팬진 방향과 회청색 배경 `#F1F5F7`을 선택했다. 배경 선택 이유는 눈이 편하다는 점이다. 굵은 로고와 게시일별 날짜 구분을 유지한다. 이전의 따뜻한 흰색 기본 배경 및 다른 감성 후보보다 이 선택을 우선한다. 비교용 메뉴는 최종 화면 요소가 아니다.

| 상태 | 내용 |
|---|---|
| 사용자 선택 | 프로젝트명 `서연모음.zip`, 저장소명 `seoyeon-zip` |
| 사용자 선호 확인 | Lomography, Frame Set, Same Energy를 디자인 레퍼런스 후보로 선호 |
| 대화에서 채택한 방향 | 윤서연과 연결되는 밝은 하늘색을 디자인 출발점으로 사용 |
| 제안 기본값 | 사진 중심의 정돈된 아카이브, 작은 날짜·출처 라벨, 밝은 테마 |
| 제안 기본값 | Frame Set에서 탐색 구조, Lomography에서 사진과 정보의 조합, Same Energy에서 사진 밀도를 참고 |
| 사용자 선택 (2026-09-09) | A안: 여유 있는 3열 피드. 선택 이유는 넉넉한 여백과 이미지 집중도. 데스크톱 최대 1200px, 열 간격 24px, 행 간격 32px. 태블릿 3열, 모바일 2열 유지 |
| 시안에서 결정 | 메인 레퍼런스의 비중, 정확한 글꼴 조합, `.zip` 로고 표현 |
| 미확정 | 모든 사진을 네컷으로 꾸미기, 인화지·필름 장식, 날짜별 시각적 그룹 |

특정 서비스의 디자인을 그대로 복제하는 것이 목표가 아니다. 아래 참고 요소는 설계자의 적용 제안이며, 해당 서비스의 모든 화면을 실측했다는 의미가 아니다.

## 3. 레퍼런스와 적용 범위

### Lomography — 사진과 기록

- 참고 화면: https://www.lomography.com/photos
- 살필 요소: 사진 목록, 최근/인기 탐색, 사진과 촬영 정보의 연결.
- 적용 제안: 사진 아래 짧은 계정명·날짜·종류를 배치해 기록이 쌓이는 느낌을 만든다.
- 가져오지 않을 기능: 장비·필름·국가 필터, 커뮤니티 순위, 인기 점수.

### Frame Set — 미디어 탐색 구조

- 참고 화면: https://frameset.app/search
- 소개·예시: https://site.frameset.app/
- 살필 요소: 프레임 탐색, 필터, 사진과 영상 관련 자료의 표시.
- 적용 제안: 목록 위의 간결한 필터와 미디어 중심 정보 계층의 출발점으로 삼는다.
- 가져오지 않을 기능: AI 검색, 영화 전문 메타데이터, 유료 업셀, 프로젝트 보드.
- 접근 제한으로 실제 화면을 보지 못하면 소개 자료와 추정 구성을 구분한다.

### Same Energy — 사진 탐색의 밀도

- 참고 화면: https://same.energy/
- 살필 요소: 이미지 중심 탐색에서 설명과 조작부의 비중.
- 적용 제안: 큰 장식 없이 사진이 화면의 대부분을 차지하게 하고, 불필요한 카드 외곽을 줄인다.
- 가져오지 않을 기능: 유사 이미지 검색, 관련 콘텐츠 추천.

레퍼런스의 현재 UI는 달라질 수 있다. 구현 때 실제 확인한 화면과 날짜, 가져올 요소를 `docs/design-review.md`에 짧게 남긴다. 레퍼런스 이미지 자체를 제품 콘텐츠로 배포하지 않는다.

## 4. 디자인 원칙

1. 첫 화면에서 실제 미디어 목록을 볼 수 있어야 한다. 큰 소개 배너가 목록을 밀어내지 않는다.
2. 사진 색과 원래 비율을 존중한다. 원본에 색 필터나 필름 효과를 적용하지 않는다.
3. 하늘색은 선택과 정체성을 드러내는 포인트다. 화면 전체를 채색하지 않는다.
4. 게시물의 출처와 시각을 쉽게 찾을 수 있어야 한다.
5. 수집 실패, 빈 결과, 미리보기 실패는 서로 구분한다.
6. 장식보다 사진의 배치·한글 타이포·간격의 일관성으로 완성도를 만든다.
7. 목록 탐색 순서와 키보드 순서가 일치해야 한다.

2026-09-09 A안 비평 반영: 비교 바와 별도 감성 소개 문구는 실제 피드에서 제거한다. 작성자와 발견 출처가 같으면 한 번만 표시하고 다르면 둘 다 표시한다. 원문 데이터는 보존하되 화면 캡션에서는 반복 그룹·멤버 해시태그를 정리한다. 마지막 저장 시각은 목록 위에서 보여주며 전체 수집 완료로 표현하지 않는다.

피할 패턴: 대형 그라데이션 히어로, 중첩된 카드, 모든 요소의 캡슐화, 무관한 통계 카드, 상시 반짝임, 사진별 무작위 회전, 반복적인 스크롤 등장 효과.

## 5. 색상 — 초기 토큰

아래 HEX 값은 시안을 위한 토큰이다. `#22AEFF`는 COSMO·Objekt 색상 팬 정리 자료에서 윤서연 값으로 확인한 디자인 기준색이며, 소속사의 공식 브랜드 매뉴얼을 직접 확인한 값으로 표현하지 않는다.

참고: https://hackmd.io/@aomou/triplescolors

| 토큰 | 초기값 | 용도 |
|---|---|---|
| background | `#F1F5F7` | 사용자 선택: 눈이 편한 회청색 기본 배경 |
| surface | `#FFFFFF` | 메뉴·설정 패널·필요한 카드 배경 |
| brand | `#22AEFF` | 로고 디테일, 선택 인디케이터 |
| brand-soft | `#EAF7FF` | 선택 필터의 배경 |
| brand-ink | `#086BA8` | 링크, 선택 필터의 글자, 포커스 후보 |
| text | `#20252B` | 본문과 제목 |
| text-muted | `#5C6670` | 날짜, 출처 보조 정보 |
| border | `#DDE3E8` | 얇은 구분선 |
| warning | `#8A4B0F` | 수집 지연과 주의 상태 |
| danger | `#B42318` | 오류·실패 메시지 |

브랜드 하늘색 위에 작은 흰색 글자를 넣는 조합을 기본으로 쓰지 않는다. 작은 본문은 진한 글자색을 사용한다. 실제 배경별 대비를 검수하고, 일반 텍스트 4.5:1·큰 텍스트 3:1을 목표로 한다. 상태를 색만으로 구분하지 않는다.

색상은 CSS 변수로 중앙 관리한다. 임의의 하늘색을 컴포넌트마다 추가하지 않는다. 다크 모드는 V1 필수 범위에 넣지 않는다.

## 6. 타이포그래피와 로고

### 초기 글꼴 제안

- 본문/제목: Pretendard 우선, 사용 불가 시 한국어 시스템 sans-serif fallback.
- 날짜/개수/`.zip`: 고정폭 글꼴을 소량 사용하거나 본문 글꼴의 tabular numbers로 처리.
- 추가 글꼴은 시안에서 가치가 확인될 때만 도입. 폰트 라이선스와 로딩 비용을 확인한다.

| 역할 | 초기 크기 | 표현 |
|---|---|---|
| 로고 | 모바일 22px, 데스크톱 26px | 600~700 weight |
| 필터·버튼 | 14px | 500 weight |
| 카드 캡션 | 14px | line-height 1.5 |
| 계정·날짜 | 12~13px | 읽을 수 있는 대비 |
| 상태 메시지 | 14px | 간결한 한글 |

한글 제목에 영문 디스플레이 글꼴의 자간을 그대로 적용하지 않는다. 긴 계정명은 한 줄 생략, 캡션은 최대 두 줄을 기본으로 하되 원문 이동을 제공한다.

로고 후보는 `서연모음.zip` 한 줄 워드마크다. `.zip`만 하늘색으로 강조하는 안과 작은 하늘색 라벨로 표현하는 안을 비교할 수 있다. 네 칸 심볼은 선택 요소이며, 로고 자체에 반드시 추가할 필요는 없다.

## 7. 메인 피드 구조

위에서 아래 순서:

1. 헤더: 로고, 목록 새로고침, 설정.
2. 필터: `전체 / 사진 / 영상`, 콘텐츠 종류 선택, 출처 선택.
3. 상태 줄: 마지막 완료 갱신 시각, 필요한 경우 일부 출처 지연.
4. 미디어 목록.
5. 더 보기 또는 목록 끝 표시.

콘텐츠 종류: 전체/COSMO/직찍/공식/기타. 실제 데이터가 없는 분류를 보여주더라도 빈 결과 상태를 정확히 표시한다. 전체 검색창은 V1에 추가하지 않는다.

목록 새로고침은 저장된 목록을 다시 읽는다. 즉시 수집을 의미하는 문구나 완료 애니메이션을 쓰지 않는다. 수집 재시도는 설정의 소스별 기능이다.

데스크톱의 고정 사이드바는 기본안에서 제외한다. 모바일에서는 보조 필터를 메뉴나 시트로 접을 수 있으며, 선택 상태와 초기화가 보여야 한다.

## 8. 미디어 목록과 반응형

### 배치 후보

| 크기 | 초기 제안 |
|---|---|
| 모바일 | 2열, 좌우 여백 12px, 열 간격 8~12px |
| 태블릿 | 3열, 좌우 여백 20px |
| 데스크톱 A — 채택 | 3열, 최대 폭 1200px, 열 간격 24px / 행 간격 32px |
| 데스크톱 B — 비교 기록 | 4열, 최대 폭 약 1440px, 간격 12~16px. 기본 피드에 채택하지 않음 |

사용자가 선택한 A안의 데스크톱 배치를 기본 규격으로 사용한다. 모바일은 좌우 12px, 열 간격 10px / 행 간격 26px의 2열을 유지한다. 360/390/768/1440px 검수 기록은 docs/design-review.md를 따른다.

기본 시안은 행 우선 CSS Grid로 만들어 화면·DOM·키보드 탐색 순서를 맞춘다. 각 사진은 원래 비율로 보이고 카드 높이는 콘텐츠에 따른다. 다른 비율 때문에 생기는 행 여백을 실제 표본으로 평가한다. 시각적 밀도를 이유로 이미지의 얼굴·워터마크를 임의로 자르지 않는다.

Masonry는 자동 확정하지 않는다. 필요성이 확인된 경우에만 읽기 순서·접근성·구현 비용을 비교한 후 선택한다. 날짜별 큰 구분은 선택 후보이며, 기본은 카드별 날짜를 가진 연속 최신순 목록이다.

## 9. 미디어 카드

선택된 팬진 방향의 사진 모서리는 사용자 요청에 따라 6px 라운드를 적용한다. 원본 비율과 사진 크기는 유지한다.

구조: 미리보기 → 출처/종류 → 날짜 → 선택적 짧은 캡션.

- 카드 외곽 박스와 그림자를 기본으로 두지 않는다. 사진과 정보 사이 간격으로 묶는다.
- 모서리는 4~8px 범위에서 시안 비교. 둥근 정도를 컴포넌트별로 제각각 쓰지 않는다.
- 이미지 크기를 알면 aspect ratio를 먼저 예약한다. 알 수 없으면 placeholder를 사용해 이동을 줄인다.
- 한 글 여러 장: 첫 미리보기 + `사진 4장` 또는 추가 장수 `+3`. 두 표기를 섞지 않는다.
- 영상: 썸네일에 재생 아이콘과 `영상` 텍스트. 앱 내 플레이어로 오해하지 않도록 원문 이동임을 접근성 이름에 포함한다.
- 사진+영상: 두 종류임을 드러내는 작은 배지. 추가 카드를 만들어 같은 글을 중복 표시하지 않는다.
- 원문 링크는 키보드로 접근 가능한 anchor로 구현한다. 중첩 링크/버튼은 피한다.
- 카드 hover에서 가벼운 경계·색 변화만 사용한다. 확대나 이동으로 주변 카드가 밀리지 않는다.
- 출처 표시와 원문 링크는 반드시 유지한다. 원본 재업로드 계정과 발견 소스를 혼동하지 않는다.

V1은 원문 새 탭 이동을 기본으로 한다. 외부 이미지 프록시, 자체 영상 재생, 모든 카드에 SNS embed 삽입은 도입하지 않는다.

## 10. 상태별 표현과 문구

| 상태 | 표현 예시 | 동작 |
|---|---|---|
| 초기 로딩 | 사진 자리와 짧은 정보 줄 skeleton | 과도한 반짝임 없이 표시 |
| 최초 데이터 없음 | “아직 모인 게시물이 없어요.” | 수집 대기/미연결 상태를 추가로 구분 |
| 필터 결과 없음 | “이 조건에 맞는 게시물이 없어요.” | 필터 초기화 |
| 일부 출처 실패 | “일부 출처의 갱신이 지연되고 있어요.” | 기존 피드 유지, 설정에서 상세 확인 |
| 목록 API 실패 | “목록을 불러오지 못했어요.” | 다시 시도, 이미 표시된 목록은 유지 |
| 미리보기 실패 | 중립 배경 + “미리보기를 불러오지 못했어요.” | 원문 보기 유지 |
| 수동 등록·미확인 | “직접 추가한 링크” | 실제 미리보기/게시 시각이 확인됐다고 표시하지 않음 |
| 명시적인 삭제·비공개 | “현재 원문을 확인할 수 없는 게시물이에요.” | 미디어 노출 중단, 상태 구분 |
| 따라잡기 중 | “이전 게시물을 확인하고 있어요.” | 마지막 완료 갱신과 구분 |

빈 결과와 접근 차단을 같은 “게시물 없음”으로 처리하지 않는다. 브라우저가 이미지를 못 읽었다는 이유로 삭제된 게시물이라 단정하지 않는다. 작성 시각이 없으면 등록 시각임을 명시한다.

## 11. 설정 화면

데스크톱은 간결한 패널, 모바일은 전체 화면 패널을 기본 제안으로 한다.

- 소스별 계정명, 활성 상태, 마지막 완료 갱신, 문제 상태.
- 활성/중지와 재시도 요청.
- 수동 원문 링크 추가.
- 메타데이터 내보내기.

재시도 문구는 `재시도 요청`으로 사용한다. 요청 직후 실제 수집 완료처럼 보이지 않게 한다. 일반 화면에 HTTP 상태나 cursor 같은 구현 용어를 노출하지 않고, 필요한 경우 오류 상세에만 제공한다.

## 12. 움직임과 접근성

- 필터·버튼 전환은 초기 120~180ms 범위. 큰 이동 애니메이션은 제외한다.
- reduced-motion 설정을 존중한다.
- 키보드 포커스를 항상 식별 가능하게 한다. 밝은 브랜드 색만으로 포커스를 만들지 않는다.
- 모바일 조작 영역은 약 44px 이상을 목표로 한다.
- 선택된 필터는 배경색 외에 글자 굵기/선택 상태를 함께 제공한다.
- 이미지 alt는 작성자·종류 등 실제 확인된 정보로 작성한다. 사진 내용을 보지 않고 상세 묘사를 만들어내지 않는다.
- 화면 리더에 메뉴·필터·외부 링크의 역할을 제공한다.
- 고정 헤더를 사용할 경우 포커스 항목과 콘텐츠가 가려지지 않는지 확인한다.

## 13. 시안 제작과 결정 절차

본 구현 전에 같은 콘텐츠로 메인 화면 A/B를 만든다. 이는 기존 디자인 구상을 다시 시작하는 절차가 아니라 남은 밀도·여백 선택을 위한 비교다.

- A: 여백 있는 데스크톱 3열.
- B: 촘촘한 데스크톱 4열.
- 두 안은 같은 색·사진·정보·정렬을 사용한다. 여러 변수를 동시에 바꾸지 않는다.
- 모바일은 두 안 모두 확인한다. 데스크톱 선호를 모바일에 기계적으로 적용하지 않는다.

표본에는 세로 인물 사진, 가로 단체 사진, 정사각형 이미지, 영상, 여러 장 게시물, 긴 한글 캡션, 긴 계정명, 미리보기 실패를 포함한다. 실제 수집 표본을 우선 사용하며, fixture는 테스트 데이터임을 분명히 하고 운영 수집 결과로 표시하지 않는다.

2026-09-09 사용자가 A안을 선택했다. “여유롭고 이미지가 눈에 잘 들어온다”는 이유로 A의 열 수와 간격을 채택한다. 글꼴과 로고는 이번 밀도 비교에서 서로 같았으므로 별도 선택이 확정됐다고 해석하지 않는다. 비교 화면 B는 참고용으로 남기며 실제 피드는 A를 기준으로 구현한다.

## 14. 검수 기준

- 첫 화면에 미디어가 보이며 불필요한 소개 영역이 없다.
- 실제 사진이 장식보다 눈에 먼저 들어온다.
- 하늘색은 일관되게 사용되고 텍스트 대비가 충분하다.
- 원본 비율, 얼굴, 워터마크를 임의로 자르지 않는다.
- 360px 화면에서 가로 넘침과 조작부 겹침이 없다.
- 긴 계정/캡션, 여러 장, 영상, 깨진 이미지가 레이아웃을 무너뜨리지 않는다.
- 필터 변경, 더 보기, 원문 이동, 설정 열고 닫기가 키보드·터치로 동작한다.
- 수집 실패가 정상 갱신처럼 보이지 않는다.
- 새로고침 이후 필터 상태는 기능 설계서의 URL 계약을 따른다.
- 실제 스크린샷을 확인하고 디자인 완료를 보고한다. 브라우저 테스트 통과만으로 미적 검수를 대체하지 않는다.

## 15. Codex 구현 지침

이 문서와 기능 설계서를 먼저 읽고, 구현 과정에서 디자인 의도를 유지한다. 수집 검증은 기능 설계서의 단계 0을 따른다. 시안 작업이 무료 자동 수집 성공을 의미하지 않는다.

권장 역할 분담:

- Superpowers: 기존 설계를 구현 계획으로 구체화하고 검증 관리.
- Ponytail: 합의한 기능·디자인·접근성을 유지하면서 코드와 의존성을 단순화.
- Impeccable: 레퍼런스에 맞는 시각적 구성, 비평, 최종 보정.
- Playwright: 실제 반응형 화면, 필터, 원문 링크, 실패 상태 확인.
- Cloudflare Skills: 배포 환경과 런타임 계약 검증.

### 추가 채택한 스킬

아래 세 스킬을 기존 조합에 추가한다. 이는 채택 결정이며 설치가 완료됐다는 뜻은 아니다. 전체 저장소의 모든 스킬을 상시 적용하지 않고 해당 작업에 맞춰 사용한다.

| 스킬 | 담당 | 프로젝트 적용 기준 |
|---|---|---|
| Jakub `better-colors` | 색상 체계와 대비 | `#22AEFF`의 브랜드 역할을 유지하며 실제 배경별 대비를 계산한다. 필요한 배경·선택·글자 토큰만 보완하고, 측정하지 않은 대비 수치를 보고하지 않는다. |
| Jakub `better-typography` | 글자 크기·굵기·줄바꿈·생략 | 실제 한글 캡션, 긴 계정명, 날짜 숫자로 검수한다. 일반적인 영문 타이포 규칙을 한글에 일괄 적용하지 않는다. |
| Emil `emil-design-eng` | 인터랙션과 움직임 보정 | 필터·메뉴·설정 패널의 반응을 다듬는다. 반복되는 동작은 움직임을 줄이고, 버튼 축소·easing 등은 이 문서와 실제 화면에 맞게 선별한다. |

원본 지침:

- https://github.com/jakubkrehel/skills/blob/main/skills/better-colors/SKILL.md
- https://github.com/jakubkrehel/skills/blob/main/skills/better-typography/SKILL.md
- https://github.com/emilkowalski/skills/blob/main/skills/emil-design-eng/SKILL.md

### 필요할 때만 사용하는 스킬

| 스킬 | 사용 시점 | 범위와 주의점 |
|---|---|---|
| Jakub `variant` | 13절의 첫 배치 비교 | 동일 콘텐츠의 3열/4열 두 안을 명시적으로 요청한다. 스킬 기본 3안보다 이 프로젝트의 2안 비교 요구를 우선한다. 색·글꼴까지 동시에 변경하지 않는다. |
| Jakub `break` | 미디어 카드 구현 후 | 실제 카드에 긴 캡션·미리보기 실패·좁은 폭 등의 도달 가능한 상태를 넣어 관찰한다. 임시 검수 화면은 운영에 포함하지 않는다. |
| Emil `review-animations` | 움직임 구현 후 필요할 때 | 애니메이션만 검수한다. 전체 UI·접근성·기능 검증을 대신하지 않는다. |

원본 지침:

- https://github.com/jakubkrehel/skills/blob/main/skills/variant/SKILL.md
- https://github.com/jakubkrehel/skills/blob/main/skills/break/SKILL.md
- https://github.com/emilkowalski/skills/blob/main/skills/review-animations/SKILL.md

`break`는 한 번 관찰하고 보고하는 성격의 스킬이다. 실제 렌더링 없이 예측한 문제를 관찰 결과로 기록하지 않는다. 이 스킬의 보고만으로 완료 처리하지 않고, 발견한 결함의 수정과 재검증은 기존 구현·Playwright 검증 흐름에서 수행한다.

### 단계별 사용 순서

1. Impeccable과 이 문서로 레퍼런스 및 화면 방향을 유지한다.
2. 배치 선택이 필요할 때 `variant`로 두 안을 비교한다.
3. `better-colors`와 `better-typography`로 실제 콘텐츠의 색상·글자 표현을 보정한다.
4. `emil-design-eng`로 필요한 인터랙션만 다듬는다.
5. Playwright로 화면·동작을 확인한다. 구체적인 카드 위험이나 움직임 문제가 남을 때만 `break` 또는 `review-animations`를 추가한다.

스킬 설치 여부와 참조 파일·사용법은 실제 Codex 환경에서 확인한다. 여러 디자인 스킬의 상충하는 스타일을 섞지 않는다. 스타일 스킬의 일반적인 기본값보다 이 문서의 프로젝트 맥락을 우선한다. 종합 리뷰를 스킬별로 반복하거나, 스킬을 사용하기 위해 불필요한 애니메이션·라이브러리를 추가하지 않는다.

최종 보고에는 선택한 배치, 조정한 토큰, 실제 검수 화면 크기, 남은 미확정 사항을 짧게 남긴다.

## 디자인 판단 위임 — 2026-09-09

사용자는 미세한 디자인 판단을 에이전트에게 위임했다. Impeccable, better-colors, better-typography의 지침과 본 문서/선택한 레퍼런스를 기준으로 실제 화면을 확인하고 필요한 수정까지 수행한다. 사진 밀도·정보 계층·한글 가독성·정렬·모바일 사용성을 우선한다. 둥글림과 간격의 취향 차이를 결함으로 만들지 않고, 문제가 없으면 유지한다. 사용자에게 확인할 선택은 전체 방향이 갈리는 경우로 한정한다. 최종 보고는 실제 화면과 주요 변경 이유 중심으로 한다.
