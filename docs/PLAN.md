## 검토함 필터 너비 안정화 — 2026-09-10

- [x] 보조 행과 기본 행 너비 계산 분리, 필터 버튼76px 유지, 현재 선택 문구 실측으로 select 폭 조절 및 화면폭 제한.

## 검토함 UI 감사 반영 — 2026-09-10

- [x] 필터44px·라벨 기준선 통일, 계정/종류는 접히는 보조 필터로 이동. 적용 개수 표시.
- [x] 인스타 일반 안내는 검토 기준으로 통합, 반복 정상 안내는 카드에서 제거, 실제 사유 유지. 가져오기 강조 완화.

## 사진 넘기기 로딩 표시 — 2026-09-10

- [x] 공용 뷰어의 이전·다음 버튼 안에 150ms 지연 회전 표시. 완료·오류·이동·destroy 시 정리.
- [x] 기존30px 버튼 유지, 로딩 중 이동 허용, reduced-motion에서는 정적인 표시.

## 인스타 영상 분류와 검토함 필터 — 2026-09-10

- [x] Apify type/isVideo/productType 및 childPosts에서 미디어별 사진·영상·미확인 보존. 릴스는 영상 분류, 승인한 글만 피드에 표시, 원문에서 재생.
- [x] 기존 원본과 저장 URL이 일치하는 항목의 유형만 보완하고 58개 검토 상태·revision 보존.
- [x] X·인스타 게시월(KST)·미디어·작성 계정 필터, X 콘텐츠 종류, 조건별 상태 건수·페이지 초기화.
- [x] 로컬 테스트·모바일 검증. GitHub 배포 확인은 VALIDATION 최신 기록 참고.

# 단계 0 로컬 검증 계획

## 인스타 개인 검토함 — 2026-09-10

- [x] `/admin/instagram` 및 `/api/admin/instagram`에 기존 소유자 Access JWT 검증을 재사용한다.
- [x] Apify JSON 최대 100개/2MB 가져오기, 게시물 코드 중복 제거, 별도 D1 검토 상태 저장을 구현한다.
- [x] 보관·제외·보류·판단 취소, 상태별 목록, 모바일 화면, 원문 링크와 이미지 실패 처리를 구현한다.
- [x] 재가져오기 시 판단 보존, CSRF 검사, revision 충돌과 피드 미변경을 검증한다.
- [x] 기존 비공개 환경 배포 후 소유자 화면과 비로그인 접근 차단을 확인한다.

수집 자체는 Apify 시험 예약을 유지한다. 이번 화면의 가져오기는 수동 JSON 입력이며 자동 동기화·자동 피드 반영은 포함하지 않는다.

## 피드 시안 — 2026-09-09

최신 48개 로컬 스냅샷으로 A(3열)/B(4열) 비교 화면 구현 및 Playwright 4개 화면 폭 검수 완료. 사용자는 여백과 이미지 집중도를 이유로 A를 선택했다. DESIGN.md에 A의 배치 규격을 반영했다. [시안 검수](design-review.md), [실행 계획](superpowers/plans/2026-09-09-feed-preview.md) 참고. 추가 계정은 보류하며 이번 시안을 운영에 배포하지 않았다.

- [x] SPEC.md에 전체 응답 저장 후 cursor 전진, 직접 작성 글만 전용 계정 예외, 수집/역할 상태 분리를 반영한다.
- [x] 필요한 제작자 스킬과 Playwright/Wrangler 도구를 프로젝트 범위에 설치한다. Impeccable은 엔진이 없어 문서 직접 적용.
- [x] 공개 API에서 최소 메타데이터를 수집한다. 원본 미디어와 raw 응답은 저장하지 않는다.
- [x] 페이지 전체 정규화 이후 트랜잭션 저장. 중간 실패는 페이지와 cursor 모두 rollback한다.
- [x] 21개 응답, 중간 실패, 오래된 cursor, DB 재오픈 및 별도 프로세스 재시작 중복 제거를 검증한다.
- [x] 전용 계정 직접 글/재게시/인용의 이름 없는 미디어 규칙을 검증한다.
- [x] 최소 카드에서 실제 사진·영상 썸네일과 이미지 실패 시 원문 링크를 모바일/데스크톱에서 확인한다.
- [ ] 지정 계정과 소유자 이메일 확인 후 비공개 Cloudflare Worker/D1/Access 검증을 진행한다.

로컬 저장 검증은 node:sqlite를 사용한다. 이는 D1 원격 CPU/SQL/Access 통과의 대체가 아니다. 검증용 정적 카드는 완성 UI 또는 최종 디자인 시안이 아니다. GitHub 버전 관리는 사용자 승인 후 도입했다.

## 자동 수집 구현 계획 — 2026-09-09

05:03 UTC 추가 진행: 보류된 종합 3개 계정에서 윤서연+COSMO 명시 미디어 표본을 확인하고 모두 활성화했다. 이제 6개 소스가 같은 5분 예약을 공유한다. 추가 계정들의 첫 실제 Cron 저장은 미확인이고, 최신 조회는 과거 교대 중 약 60분/이후 약 30분이다. 상세 표본과 조사 범위는 VALIDATION.md 최상단을 따른다.

최신 실행 상태: 사용자 직접 결제 후 Workers Paid Active를 확인했다. 0003을 비공개 앱 DB에도 적용하고 CPU 100ms / 5분 예약을 배포했다. Access 비로그인 302·소유자 API 200·기존 20개 보존 확인 후 검증된 전용 계정 3개만 활성화했다. 04:55 UTC 첫 예약에서 Seowoo_0501의 20개 upsert / 총 21개 / CPU 10ms / outcome ok를 확인했다. 다른 2개 계정의 첫 예약 및 장기 운영은 미확인이다. 아래의 보류 문구는 이전 시점 기록이며 최신 결과는 VALIDATION.md를 따른다.

04:19 UTC 후속 결과: 사용자 로그인 후 대시보드에서 실제 검증 Cron의 CPU 13ms / wall 2348ms / outcome ok 확인. 무료 Cron 기준 10ms를 넘는 표본이므로 활성화 보류. 임시 검증 예약과 모든 수집 스위치는 제거/비활성화했다. 다음은 CPU 프로파일링·절감·재측정과 최신/과거 탐색 분리 설계다.

후속 검증에서 공급자의 게시물 변환 실패가 code=200 응답에서 제외될 수 있고, 최근 범위 밖에서도 cursor가 계속되는 점을 확인했다. 전체 탐색 완료까지 기다리는 현재 cycle은 최신 갱신 지연 가능성이 있다. 다음 설계 변경은 최신 목록의 제한된 재탐색과 과거 이어받기를 분리하고 확인 범위를 명시하는 방향으로 검토한다. CPU 로그 조회는 기존 인증에서 403이며 대시보드 인증이 필요하다. 상세 증거는 VALIDATION.md의 후속 검증 절을 따른다.

실행 결과: 핵심 코드와 로컬 검증 완료(29 tests + workerd/D1), 원격 상태 마이그레이션 적용/기존 20개 보존/전역·소스 비활성. 분리한 원격 개발 Worker/D1에서 실제 32개 저장, SQL 실패 rollback, 재시도, 중지 후 저장 없음까지 확인했다. 원격 CPU·동시성 실측과 공급자 탐색 종료 계약이 남아 Cron 활성화는 보류했다. 아래 내용은 설계 당시 요약이며 상세 실행 상태는 연결 문서와 VALIDATION.md를 따른다.

Superpowers 실행용 상세 계획: [2026-09-09-automatic-collection.md](superpowers/plans/2026-09-09-automatic-collection.md). 아래는 요약 설계이며, DB 중지 revision과 단계별 인터페이스·테스트·활성화 절차는 상세 계획을 따른다.

목표: 기존 수동 수집의 판별·응답 문제를 수정하고, 예약 수집의 구현 계약을 확정한다. 이번 범위에서 Cron 활성화나 스키마 마이그레이션은 하지 않는다.
기술: 기존 JavaScript 모듈, Workers, D1, Node test runner. 요구사항: docs/SPEC.md §6–7. 디자인는 DESIGN.md 우선.

### 현재 수정 범위

- `src/sources.mjs`: 로컬/Worker 공통 소스와 전용 직접 작성 규칙. 전용 계정 판별 허용은 자동 실행 승인과 별개다.
- `src/worker.mjs`, `scripts/collect.mjs`: 공통 설정 사용. 페이지 커밋 이후 관측 로그 실패는 HTTP 200과 `warning:observation_log_failed`로 알린다. 실패한 로그 SQL의 비용은 확정할 수 없어 rowsRead/rowsWritten을 null로 반환한다. 기존 runs 기반 수집 시각은 이 경우 오래된 값일 수 있다.
- `tests/probe.test.mjs`: 이름 없는 직접 글 포함/재게시·인용 제외, 커밋 후 로그 실패, stale batch 409를 검증한다. DB 대역 테스트이므로 원격 D1 원자성 검증을 대체하지 않는다.

### 실행 모델과 운영 봉인

- Cron 제안: UTC 기준 `*/5 * * * *`. 한 실행은 due 소스 하나, 공급자 페이지 하나만 처리한다. 6개 정상 소스의 목표 조회 간격은 약 30분이나, 초기 수집·재시도 때문에 길어질 수 있다. 1일 기본 호출 슬롯은 288개다.
- UI 새로고침은 DB 조회만 한다. 수동 재시도는 next_due_at 조정만 하고, 자동 운영 시작 후 기존 `/api/probe`의 직접 저장 경로는 비활성화한다. 모든 writer는 동일 lease/커밋 함수로 통합한다.
- `COLLECTION_ENABLED`가 명시적으로 true이고 소스 enabled도 true일 때만 외부 호출한다. 기본값은 모두 false. HTTP Access 설정은 유지하지만 scheduled 실행에는 JWT가 없으므로 Access를 예약 실행의 차단 장치로 간주하지 않는다.
- 실행 중 전역/소스 중지 전환도 커밋 전에 확인한다. 중지와 함께 revision을 변경해 오래된 writer가 커밋할 수 없게 한다. 중지 시 이미 저장된 게시물은 보존한다.

### 상태와 최초 전환

- 새 `migrations/0002_collection_state.sql`: 기존 posts/media/discoveries는 유지. 운영 상태는 별도 테이블로 만들어 검증용 cursor를 완료 경계로 재사용하지 않는다.
- 상태 필드: source, enabled, revision, next_due_at, lease_token, lease_until, last_attempt_at, last_success_at, last_complete_sync_at, committed_boundary_at, cycle_started_at, cycle_boundary_at, next_cursor, pages_in_cycle, failures, last_error_code, catchup_status.
- 최초 활성화는 서버 시각 기준 최근 7일을 고정 하한으로 설정. 기존 게시물은 ID upsert로 중복 제거한다. 이미 누락된 이름 없는 직접 작성 글도 해당 범위 재탐색으로 복구하며 더 오래된 글의 복구는 보장하지 않는다.

### 새 글과 이어받기

1. cycle이 없으면 첫 페이지부터 시작한다. 완료된 경계에서 24시간 겹침을 뺀 값을 cycle_boundary_at으로 고정한다. cycle_started_at은 이번 사이클 시작 시각으로 고정한다.
2. 진행 중이면 저장된 next_cursor로 이어받는다. page 수/요청 count로 결과를 잘라내지 않는다. 받은 모든 글을 정규화·판별하고 해당 페이지 저장과 상태 전진을 하나의 batch에 넣는다.
3. 판별 탈락 글을 포함한 공급자 원래 목록을 보고 탐색 진행을 판단한다. 한 개의 고정 글/오래된 글, 대상 글 0개, 이미 저장된 ID 한 개만으로 종료하지 않는다.
4. 공급자의 재게시 시각·정렬·고정 글 특성은 아직 충분히 검증되지 않았다. 이 계약이 검증되기 전에는 작성 시각만으로 경계 도달을 판정하지 않는다. cursor 소진 또는 검증된 경계 도달 조건이 있어야 complete로 처리한다. 공급자가 범위를 주지 않으면 gap/needs_attention을 기록한다.
5. 완료할 때만 committed_boundary_at을 cycle_started_at으로 전진하고 cursor/cycle을 비운다. 다음 정상 due는 30분 후다. 각 페이지 저장 시각과 전체 탐색 완료 시각은 별도로 보여준다.
6. 20페이지 이후에도 경계 미도달이면 catchup을 표시하고 완료 경계는 유지한다. 다음 슬롯에 계속하되 다른 소스와 교대한다. 새 cycle을 무한히 덮어쓰지 않는다. 따라잡기 중 새 글 조회 지연을 상태로 드러낸다.
7. cursor 만료가 명시된 응답이면 동일 cycle 경계를 유지한 채 첫 페이지로 재시작하고 ID로 중복 제거한다. 반복 만료/동일 cursor 반복은 제한 횟수 후 needs_attention. 임의의 400을 만료로 추정하지 않는다.

### 동시성·실패 계약

- due 소스는 next_due_at, last_attempt_at, source 순으로 선택한다. 조건부 UPDATE … RETURNING으로 lease를 원자적으로 획득한다. 조회 후 무조건 UPDATE하는 방식은 금지한다.
- lease 제안값 120초, 외부 요청 타임아웃 15초. DB 시각을 사용한다. fetch를 DB 트랜잭션 안에서 대기하지 않는다.
- 페이지 batch 시작 guard는 source enabled, lease_token, 미만료 lease, expected revision을 모두 확인한다. posts/media/discoveries, 다음 상태와 lease 해제를 동일 batch에 넣는다. 실패/백오프 상태 갱신도 같은 소유권 조건이 필요하다. 늦게 끝난 이전 실행은 새 실행 상태를 변경하지 못한다.
- 프로세스 종료 시 lease 만료 후 같은 미완료 페이지를 재시도한다. telemetry 실패는 커밋을 되돌리거나 재시도를 예약하지 않는다. last_success_at은 운영 상태 batch에 저장해 runs 로그와 분리한다.
- 429는 Retry-After(초/HTTP 날짜)를 파싱해 우선 적용. 없거나 잘못되면 30분→1시간→2시간→최대 6시간 백오프. 타임아웃/5xx도 동일. 성공 시 failures 초기화.
- 401/403, 스키마 변경은 needs_attention으로 중단한다. 204는 공급자의 since 계약과 현재 cycle에 맞을 때만 변경 없음으로 처리하며 진행 중 cursor를 지우지 않는다. 2MiB 초과 등 안전 제한 실패는 checkpoint 유지 후 needs_attention으로 표시한다.

### 구현 순서와 검증 기준

1. 상태/lease (`migrations/0002_collection_state.sql`, `src/scheduler.mjs`, `tests/scheduler.test.mjs`): 동시 획득, 만료 후 새 소유자, 늦은 응답, 중지 중 커밋 거절 테스트를 먼저 만든 뒤 구현. 기존 검증 DB 마이그레이션과 재실행 안전성 확인.
2. cycle/저장 (`src/worker.mjs`, `src/collection.mjs`, `src/scheduler.mjs`, `tests/scheduler.test.mjs`): 21개 응답, 전부 판별 탈락한 페이지, 페이지 중간 실패, 재시작, 오래된 고정 글, cursor 반복/만료, 20페이지 catchup, 로그 실패를 검증. 실제 D1 batch rollback도 비공개 환경에서 확인.
3. 오류 상태/예약 진입점 (`src/worker.mjs`, `src/collection.mjs`, `tests/scheduler.test.mjs`): 구조화된 provider 오류와 Retry-After를 반환하도록 확장. scheduled는 `runDueSource(env)`를 기다리고, 기본 비활성/6소스 공정성/한 페이지 상한 테스트. public 수집 우회 URL을 추가하지 않는다.
4. 로컬 테스트 `npm test`, `npx wrangler deploy --dry-run` 후 비공개 수동 운영 검증. 정상·미디어 다수·중복·재시도 CPU/SQL을 실측하고 한도 적합성을 판단한다. 무료 한도 미충족 시 자동 활성화를 보류하며 유료 전환하지 않는다.
5. 6소스의 역할 검증 상태를 각각 기록한다. 목록 성공과 COSMO 역할 성공을 분리한다. 검증을 통과한 소스만 enabled로 변경하고 마지막으로 `wrangler.jsonc` Cron을 추가한다. GitHub push가 배포를 유발하므로 구현 커밋과 활성화 변경을 분리한다.

공식 근거: [Cron Triggers](https://developers.cloudflare.com/workers/configuration/cron-triggers/), [D1 batch](https://developers.cloudflare.com/d1/worker-api/d1-database/). 위 주기·lease·백오프는 프로젝트 설계값이며 플랫폼 보장값이 아니다.

## 실제 피드 연결 — 2026-09-09

- [x] 확정된 A/팬진/회청색/Phosphor를 기본 화면으로 설정.
- [x] 인증 이후 `/api/feed`에서 전체 저장 데이터의 미디어·종류·발견 출처·한국시간 게시월 필터 적용.
- [x] 게시일과 ID 기준 cursor, 48개 페이지, 전체 일치 개수, 더 보기 연결.
- [x] 조건 변경 시 오래된 응답 무시, 조회 실패·로그인 만료 안내 구현.
- [ ] DB 실행 검증: 48개 초과, 같은 게시일 경계, 최신/오래된순, 월 경계, 복수 발견 출처, 잘못된 cursor.
- [ ] 실제 브라우저에서 빠른 필터 변경·더 보기 실패 재시도·이미지 실패 확인.
- [ ] 비공개 배포 후 Access 및 D1 SQL/CPU 확인. 이번 변경은 미배포.

로컬 4174는 기존 저장본 시안이다. Worker 기본 경로는 feed.html로 연결하며, 로컬 Workers에서 실제 API를 확인할 때는 `?data=live`로 선택한다. 새로운 DB 마이그레이션은 없으며 데이터 규모가 커질 때 JSON 조회/전체 개수 SQL 비용을 실측해야 한다.

후속 결과: 2026-09-09 06:53 UTC, 명시 승인 후 비공개 배포 및 실제 API/더 보기 검증 완료. 실제 ID 접두어 cursor 오류 수정. 전체 55개를 48+7로 중복 없이 확인. 원격 SQL/CPU 비용 실측은 별도 잔여 항목. 상세 증거는 VALIDATION.md 최하단 참조.

2026-09-09 피드 비용 1차 실측 완료: 55개 기준 Worker CPU 5ms / wall187ms 단일 GET 표본. 원격 SQL SELECT 0.18~2.89ms, 쓰기0, 기본 페이지 SQL 대응 읽기 합계171행. 현재 즉각적인 변경 없이 유지. 데이터 증가 후 정렬 인덱스와 COUNT/영상 필터 비용 개선을 검토한다. 장기/p95와 Cron 비용은 별도이며 상세 측정 범위는 VALIDATION.md 참조.

## 보조 소스 확장 적용 — 2026-09-09

사용자 승인으로 sogeumdwarf/hamhamm806/S2O806 추가 활성화(총9개). 한글 코스모톡 분류 보완. 비활성 마이그레이션→코드 배포→3행 활성화 순서 완료. 테스트37개·dry-run·Access302 확인. 첫 신규 Cron 저장은 미확인.
최신 주기 계약은 3분1소스, 하루480슬롯, 전체 약27분/초기 최신·과거 교대시 최신약54분이다. 이 절이 앞의 6소스/5분 설명보다 우선하며 자세한 근거는 VALIDATION.md 참조.

공식 등록 완료: triplescosmos 추가, 총10소스. official-v1 및 공식 검토자료 원자적 별도보존 구현/비공개 배포. 최초 공식 Cron은 대기. 3분슬롯 기준 전체약30분, 초기교대 최신약60분. 검토관리 UI는 미구현. 상세는 VALIDATION.md 공식 소스 운영 등록 절.

Pumpkin 등록 완료(2026-09-09): 사용자 확정에 따라 Pumpkin030806만 추가 활성화, 총11소스. hampuppy806 보류. 이름 기반/미디어 필수, verifiedDirect=false, 최근7일 유지. 3분슬롯 전체약33분, 초기교대 최신약66분. 첫 Pumpkin Cron 성공은 아직 미확인.

수집 상태 화면 보완 완료(2026-09-09): 마지막 성공 페이지 응답/조건통과/검토보류 건수를 원자적으로 보존하고 대기/성공/재시도/중지를 표시. 상태만 새로고침 가능. 43개 테스트·모바일/데스크톱·비공개 배포 및 로그인 화면 확인. 새 건수의 실제 Cron 저장은 미확인. 상세 VALIDATION.md 참조.

## 날짜 제한 해제 — 2026-09-10

사용자 승인: 날짜 제한 없이 과거 자료를 수집하되 20페이지 상한과 최신/과거 교대를 유지한다. 최초 경계는 epoch 0, 최신 첫 페이지도 날짜 제한을 해제한다. 기존 판별·공식 검토 분리·Access·3분 예약은 유지한다.

전환 순서: 코드 검증 → GitHub 커밋/푸시 및 자동 배포 성공 확인 → 0008 원격 적용 → 실제 Cron 결과 확인. 0008은 기존 게시물을 보존하고 cursor/페이지 수를 초기화해 건너뛴 페이지를 재탐색한다. revision 증가와 lease 해제로 이전 writer를 차단한다. enabled, needs_attention, retry 백오프는 보존한다. 마이그레이션은 한 번만 적용한다.

11소스 최신 순회는 과거 교대 중 약66분이며 전체 과거 수집 완료를 보장하지 않는다. 결과는 VALIDATION.md에 기록한다.

적용 결과: b1f6bb1 자동 배포 및 0008 적용 완료. 첫 Cron에서 83→87개, 가장 오래된 자료 2026-08-29로 확장 확인. 20페이지 전체 종료는 아직 미확인. 상세 VALIDATION.md 참조.

## X 피드 품질 — 2026-09-10
사진별 원본 SHA 중복 제거, 유사 사진 수동 묶기, 안내·상품 검토, 원문 상태 재확인 및 소유자 X 검토함을 구현. 원본 보존. 상세 계획: docs/superpowers/plans/2026-09-10-x-quality.md.

## 인스타 피드 연결 — 2026-09-10
승인 범위: kept만 통합 날짜순 피드에 표시, 검토 문구를 표시 중/피드에 표시로 변경. 공통 뷰어와 Instagram 출처 필터. 0012 조회 뷰를 먼저 추가한 뒤 코드 자동 배포. SHA가 같은 사진은 X 우선 대표로 합치고 인스타 출처 연결, 다른 사진은 유지. 예약 유지관리가 보관된 인스타 이미지 SHA를 순차 확인. 재압축/크롭 유사 사진은 자동 확정하지 않음. 기존 15개 kept는 첫 사진만 저장돼 전체 이미지 재수입이 별도로 필요.

## 관리 기능 — 2026-09-10
소유자 수집 상태 dialog에 소스별 활성/중지, 원문 URL 링크카드 등록, 정규화 메타데이터 JSON 다운로드 추가. revision 확인과 lease 무효화로 중지 이전 수집 저장 차단. 전체 수집 봉인/오류 백오프/cursor 유지. 0013 additive view를 먼저 적용한 뒤 자동 배포. 인스타 자동 동기화는 별도 작업.

## 2026-09-10 관리 모달 정리
- 수집 / 자료 관리 탭 분리, 고정 헤더와 내부 스크롤, 아이콘 닫기/새로고침.
- 계정별 제어를 제목 행에 배치하고 상세는 접기. 결과는 해당 계정에 표시하며 포커스 복구.
- URL 입력창과 버튼 스타일 통일. 기존 API/데이터 동작 유지.

## 인스타 결과 자동 연결 — 2026-09-10
기존 Apify task의 성공한 실행 결과를 5분마다 최대 10행씩 검토함에 가져온다. 새 글은 pending, 기존 판단은 유지한다. 실행별 checkpoint와 게시물 저장은 같은 트랜잭션으로 처리하며, 실패 실행은 지연 재시도하여 다음 정상 실행을 막지 않는다. 새 유료 수집 실행이나 기존 Apify 예약·비용 상한 변경은 없다. 0015 원격 적용 완료. APIFY_TOKEN Secret 등록 전에는 비활성 대기하며 실제 연결 검증은 등록 후 진행한다.

## WEV86_ 고정 수집 등록 — 2026-09-10
사용자 승인으로 WEV86_ 추가. 이름/해시태그+미디어 조건 유지, verifiedDirect=false. 0016 비활성 등록→코드 자동 배포→활성화 순서. 총12소스, 3분 슬롯 전체 순회 약36분(최신/과거 교대 시 최신 약72분). 날짜 제한 없이 기존20페이지 상한 유지.

## 다양성 보강 계정 — 2026-09-10
사용자 승인: merongseo806/yeoniverse_bb 고정 수집, wavefunc0806 과거 보충. 모두 기존 이름+미디어 조건, verifiedDirect=false. wavefunc0806은 최신 교대 없이 과거 페이지를 진행하고 끝/반복/20페이지 상한에서 저장과 동시에 비활성화. 등록15개, 보충 종료 후 고정14개. 기존3분슬롯 유지.

## 게시일 필터 — 2026-09-10
피드와 X/인스타 검토함의 게시월 입력을 게시일 입력으로 변경. 한국시간 해당 날짜 하루 조회, 기존 미디어/작성계정/종류 조합 유지. 기존 month API는 호환 유지하고 화면은 date 쿼리 사용.

## 검토함 버튼 크기 통일 — 2026-09-11
사용자 승인에 따라 비교 영역의 별도 font-size/min-width 예외를 제거해 기존 검토 버튼의 14px 글자, 최소 높이 44px, 여백 8px 13px, 최소 너비 76px를 공통 적용한다. 모바일 배치는 유지한다.

## 유사 사진 비교 정보 배치 보정 — 2026-09-11
사용자 기준은 기존 오른쪽 유사 게시물 배치다. 후보가 있을 때만 현재 글 정보를 사진 바로 아래로 이동하고 계정/날짜/표시 상태/원문을 12px 정보 행으로 배치한다. 버튼은 정보 행 다음, 검토 사유와 본문은 그 아래 유지. 후보 없는 사진으로 이동하면 기존 글 정보 배치를 복원한다.

## 유사 게시물 직접 숨기기 — 2026-09-11
오른쪽 버튼을 짧은 '이 글 숨기기'로 변경하고 현재 후보 게시물을 대상으로 한다. 후보의 decision/revision을 조회 응답에 포함해 기존 낙관적 잠금을 사용한다. 오른쪽 숨기기 후 왼쪽 피드 표시 허용 흐름을 지원한다.

## X 수집 중복 페이지 교착 복구 — 2026-09-11
WEV86_ 응답의 동일 게시물 반복으로 media 복합키 충돌이 나고, 저장 실패 미기록으로 같은 소스만 선택되는 문제 수정. 페이지 전체 검증 후 게시물 ID별 마지막 적격 스냅샷을 한 번 저장한다. 저장 실패는 원래 cursor를 보존한 storage_error 재시도 상태로 기록해 다른 소스 진행을 허용한다. 데이터 삭제나 cursor 초기화 없이 기존 비공개 자동 배포로 복구한다.

## 방문자·관리자 기능 분리 계획 — 2026-09-11

방문자는 피드와 읽기 전용 수집 현황을 조회한다. 중지/켜기/재시도, 자료 관리, X/Instagram 검토함은 관리자에게만 표시하고 서버에서도 접근을 차단한다. 방문자 모달은 '수집 현황', 관리자 모달은 '수집 및 관리'로 구분한다. 기존 Access 소유자 인증과 3분 수집 주기는 유지한다.

상세 설계: [방문자·관리자 기능 분리](superpowers/specs/2026-09-11-public-admin-separation-design.md). 실행 계획: [구현·검증·공개 전환 순서](superpowers/plans/2026-09-11-public-admin-separation.md).

사용자 실행 승인에 따라 구현과 로컬 검증, 비공개 자동 배포를 완료했다(코드8984db5). 전체92개 테스트와 방문자/관리자 브라우저 검증 통과. PUBLIC_FEED_ENABLED=false 및 기존 Access 보호, 수집 주기는 유지한다. 운영 관리자 화면은 사용자가 Chrome에서 검토함·수집 중지 버튼 표시를 직접 확인했다. 향후 공개 경로의 인증 쿠키 전달 확인은 남아 있으며, 실제 공개 전환은 별도 사용자 요청 이후 수행한다. 상세 결과는 VALIDATION.md를 따른다.

## 공개 전 요청 제한 보완 — 2026-09-11
사용자 승인에 따라 비공개 상태에서 공개 API 보호를 준비한다. 공개 모드의 GET /api/feed, /api/collection-status, /api/session을 Cloudflare 제공 IP 기준 공통 60회/60초로 제한한다. 초과는 DB·JWT 처리 전 429와 Retry-After:60, 바인딩 누락·장애는 503으로 닫는다. 정적 자산과 관리자 인증 경계, 수집 주기는 유지한다. 잘못된 페이지 cursor 검증은 COUNT 조회 앞으로 옮긴다. PUBLIC_FEED_ENABLED=false와 기존 Access를 유지해 자동 배포한다.
공유 IP 사용자는 같은 한도를 사용하며 Cloudflare 위치별 비동기 제한이므로 정확한 전역 비용 상한은 아니다. 공개 정책 적용 후 관리자 로그인·쿠키 전달은 실제 전환 단계에서 별도 확인한다.

## 공개 전환 실행 — 2026-09-11
사용자가 공개 전환을 명시적으로 승인했다. 기존 Access 앱의 소유자 정책·AUD·24시간 세션·HTTP Only·호스트 범위 쿠키는 유지하고 보호 대상을 /admin 경로로 좁힌다. 나머지 경로는 Worker가 정확한 공개 GET/HEAD 목록과 관리자 JWT 검증으로 통제한다. /api/* 전체 허용 규칙은 만들지 않는다. 전체 Access 보호를 유지한 상태에서 PUBLIC_FEED_ENABLED=true를 먼저 배포하고, 이후 Worker 전체 Access 대상을 제거한다. 익명 피드/현황과 관리 API 차단, 운영자 로그인 및 쿠키 전달을 운영에서 확인한다. 실패 시 같은 Access 앱에 seoyeon-zip Worker 전체 대상을 복원한 뒤 플래그를 false로 배포한다. 데이터·수집 주기는 변경하지 않는다.

공개 전환 결과(2026-09-11): 코드 ae5f3d9 배포 및 기존 Access 앱의 /admin 보호로 전환 완료. 익명 운영 HTTP 조회/관리 요청 차단, 실제 운영자 브라우저의 공개 경로 쿠키 인식·검토함/관리 현황 조회, 수집 성공 시각 전진을 확인했다. 상세 증거와 검증 범위는 VALIDATION.md의 공개 전환 완료 항목에 기록한다.

## 공개 피드 시안 흔적 정리 — 2026-09-11
탭 제목을 서연모음.zip으로 바꾸고 기본 표시 주소를 /로 정리한다. 시안용 layout/mood/icons/background/compare 옵션과 비교 선택 UI를 제거하고 확정된 zine/phosphor/mist 디자인을 고정한다. 실제 date/sort/media/kind/source 조건과 해시 링크는 유지하며 기존 /feed 링크도 읽은 뒤 주소를 정리한다.

## 로그인 아이콘 통일 — 2026-09-11
로그인 텍스트 링크를 기존 상단 icon-button 스타일의 진입 화살표 SVG로 변경한다. /admin 링크·역할에 따른 숨김은 유지하며 관리자 로그인 툴팁·aria-label을 제공한다.

## 사이트 안내·저작권 문의 — 2026-09-11
사용자가 승인한 로고 옆 안내 아이콘과 사이트 소개·출처/저작권·문의 dialog를 구현한다. 공개 승인한 운영 이메일에 mailto 및 주소 복사를 연결하고 게시물 요청은 원문 URL을 메일 본문에 포함한다. 인증/수집 동작은 변경하지 않는다.

## 안내 아이콘 수직 위치 — 2026-09-11
사용자 요청에 따라 로고 옆 안내 SVG를 버튼 안에서 6px 내려 .zip 높이에 가깝게 맞춘다. 44px 버튼과 안내 창 동작은 유지한다.

## 로고·안내 아이콘 하단 정렬 수정 — 2026-09-11
이전 6px 이동은 요청을 잘못 해석한 수정이었다. 사용자가 명확히 지정한 글자와 아이콘의 보이는 하단을 맞춘다. 로고 하단 정렬과 글자 크기별 하단 여백/SVG 내부 여백 보정을 적용하며 44px 클릭 영역은 유지한다.

## 모바일 안내 아이콘 축소 — 2026-09-11
600px 이하 화면에서 안내 SVG를 20px에서 16px로 축소한다. 축소된 SVG 내부 여백도 함께 보정하여 로고 하단 정렬과 44px 터치 영역을 유지한다.

## 공개 README 정리 — 2026-09-11
승인된 대표 문구 '모아두고 싶은 서연의 순간들.'과 전체 초안을 README에 반영한다. 공개 방문자 PC/모바일 캡처, 기능·기술·검증 안내와 출처/문의 문구를 넣는다. 기존 비공개 안내는 현재 운영과 구분한 INITIAL_VALIDATION.md 보관본으로 유지한다.

## myeongsim_ 수집 계정 추가 — 2026-09-11
사용자 승인으로 일반 이름 필터 계정 추가. verifiedDirect/historyOnly 예외 없음. 0018은 비활성 행만 생성하고 새 코드 배포 확인 후 해당 계정만 활성화한다. 기존 수집 주기·다른 계정 cursor/상태는 유지한다.

## 피드 원문·문의 아이콘 — 2026-09-11
승인된 배치대로 Phosphor Regular 외부 링크·말풍선 아이콘을 영상 안내 오른쪽에 배치한다. 사진 카드도 같은 줄 오른쪽 정렬을 사용한다. 모바일 안내는 원문에서 재생으로 줄이고 44px 터치 영역·접근성 이름·툴팁과 기존 링크/문의 동작을 유지한다.

## 피드 아이콘 밀도·정렬 보정 — 2026-09-11
사용자가 지적한 큰 조작 영역과 불규칙한 간격을 수정한다. 피드 아이콘 16px/버튼 28px, 조작 행 위 6px·작성자 정보 위 6px로 정리하고 안내/아이콘 수직 중앙 정렬을 유지한다. 상단 아이콘 크기는 유지한다.

## 피드 아이콘 오른쪽 정렬 — 2026-09-11
사용자 요청에 따라 버튼 내부6px와 Phosphor SVG 오른쪽1.5px 여백을 보정해 마지막 말풍선의 보이는 끝을 사진·분류의 오른쪽 정렬선에 맞춘다. 기존 아이콘 크기·행 높이는 유지한다.

## README 최신 아이콘 화면 갱신 — 2026-09-11
최종 아이콘 크기·오른쪽 정렬이 반영된 공개 피드의 PC/모바일 캡처를 기존 README 이미지 경로에 갱신한다.

## 상단 로그인·검토함 오른쪽 정렬 — 2026-09-11
마지막 표시 아이콘의 실제 오른쪽 끝을 헤더 구분선과 맞춘다. 로그인/검토함의 서로 다른 SVG stroke 여백을 상태별로 보정하고 상단 아이콘 간 간격과 44px 버튼 크기를 유지한다.

## README 상단 정렬 화면 갱신 — 2026-09-11
상단 로그인 아이콘 오른쪽 정렬까지 반영된 공개 화면으로 README의 PC·모바일 이미지를 갱신한다.

## UI 감사 지적 1·2 수정 — 2026-09-11
상단·피드 아이콘의 translateX 보정을 제거하고 실제 도형이 각 버튼 내부 오른쪽에 놓이도록 SVG 여백만 보정한다. SVG pointer-events를 끄고 모바일 상단 버튼 간격2px로 인접 클릭 영역 간섭을 방지한다. 사진 확대 aria-label과 가져오기 창 제목 aria-labelledby를 추가한다.

## 좁은 화면 검토함·가져오기 보완 — 2026-09-11
검토함 상태 탭은 줄바꿈으로 모든 항목을 표시하고 가져오기 닫기 버튼의 줄바꿈을 막는다. 모바일 dialog 여백·제목 크기를 조정한다. 빈 입력 제출 오류는 기존 경고 색상을 사용한다. 사용자가 빈 입력 제출 검사를 승인했다.

## 아이콘과 버튼 중심 일치 — 2026-09-11
사용자 승인에 따라 SVG만 이동하는 정렬을 제거하고 버튼 전체 그룹을 이동한다. 상단44px/22px·피드28px/16px 중앙 정렬을 유지한다. PC에서는 마지막 도형 끝을 콘텐츠 끝과 맞추고 모바일은 그룹 이동6px로 제한해 터치·포커스 영역의 화면 내부 배치를 우선한다.

## 더 보기 포커스·입력 오류 연결 — 2026-09-11
사용자 요청에 따라 더 보기에서 첫 새 게시물로 키보드 탐색을 이어가고 마지막 페이지도 처리한다. 로딩 중 다른 곳으로 포커스를 이동했다면 새 게시물로 강제 이동하지 않는다. JSON 파싱 오류를 입력란에 연결하고 오류 입력란으로 이동하며 편집 시 이전 오류를 해제한다. 실제 스크린리더 음성 청취는 사용자 요청으로 후속 검증으로 미룬다.

## 사이트 안내 아이콘 하단 정렬 — 2026-09-11
사용자가 표시한 로고 하단선을 기준으로 안내 아이콘의 하단을 맞춘다. SVG 개별 이동을 제거하고 44px 버튼 전체 위치를 조정해 클릭 영역과 아이콘 중심을 유지한다.

## 검토 결정 감사 로그 — 구현 및 배포 완료
사용자 요청으로 [감사 로그 및 검토 내역 계획](superpowers/plans/2026-09-11-review-audit-log.md)을 작성했다. 현재 상태와 append-only 이벤트 분리, 원자적 저장, 인증된 검토자, 사진 비교 당시 근거, 관리자 조회 화면을 포함한다. 사용자가 계획 커밋과 구현을 승인했다. 현재 상태/로그의 원자적 저장, 이유 선택, 관리자 내역 조회를 구현했으며 검증 및 운영 결과는 VALIDATION.md에 기록한다.

## 검토 내역 UI 정리 — 2026-09-11
사용자 요청의 여섯 항목을 반영한다. 검토함의 내역 링크와 내역 페이지의 플랫폼 이동·피드 복귀·새로고침을 기존 SVG 아이콘/툴팁 패턴으로 통일한다. 필터 폭과 높이를 줄이고 모바일은 두 열·44px 조작 영역을 유지한다. 안내문은 화면 폭에 맞춰 자동 줄바꿈한다.

## 검토 내역 UI 감사 후속 수정 — 2026-09-11
더 보기 대기 중 사용자가 옮긴 포커스를 유지한다. 버튼에 남아 있는 키보드 사용자는 마지막 페이지에서도 첫 새 항목으로 이동한다. 터치/혼합 입력 기기의 필터 높이는 화면 폭과 무관하게44px 이상으로 유지한다.

## 검토 화면 새로고침 정렬 — 2026-09-11
X·Instagram·검토 내역의 새로고침 버튼 전체를 오른쪽으로 이동해 SVG의 보이는 끝을 콘텐츠 정렬선에 맞춘다.44px 클릭 영역과22px SVG의 중심을 유지한다. 모바일은12px 이동으로 화면 내부 클릭 영역을 우선하고 아이콘 끝은 약1.84px 안쪽에 둔다. 포커스 링은 버튼 안쪽에 표시한다.

## 검토 내역 현재 페이지 아이콘 — 2026-09-11
검토 내역 페이지에도 내역 아이콘을 표시하고 aria-current=page로 강조한다. 세 검토 페이지 모두 같은3개 아이콘을 유지하고 마지막 내역 SVG의 실제 오른쪽 끝에 맞춰 헤더 그룹 이동량을 보정한다.

## 검토 내역 썸네일 목록 — 2026-09-11
검토 시각 아래 사진 썸네일을 배치하고 원문 링크를 아이콘으로 표시한다. 상세 보기 버튼은 본문 가까이에 배치한다. 새 판정은 서버의 사진 주소를 기록하며,이미 저장된 기록은 변경하지 않고 현재 게시물에서 미리보기를 조회한다. 당시 주소/현재 미리보기 출처를 구분한다.

## 2026-09-11 검토 내역 목록 아이콘 정렬
- 요청: 원문 아이콘의 왼쪽 시각 경계를 판정 이유 글자 시작선에 맞추고 상세 보기를 문서 SVG 아이콘으로 변경.
- 두 아이콘은 44px 클릭 영역과 툴팁/접근성 이름을 유지. 상세 열기 및 포커스 복귀 동작 유지.

## 2026-09-11 홈 화면 웹 앱
- 이름 `서연모음.zip`, 시작 URL/ID/scope `/`, `display: standalone`. 공개 피드에 manifest 및 Apple 홈 화면 메타데이터 연결.
- 기존 아이스바 모티프를 단순 SVG로 다시 제작(`docs/assets/home-screen-icon.svg`), 회청색 불투명 배경의 PNG 180/192/512px 제공. 소스 SVG는 배포 자산에 포함하지 않음.
- 설치용 네 파일의 정확한 GET/HEAD 경로만 공개 허용. 오프라인 캐시와 서비스 워커는 이번 범위에 포함하지 않음.
- 참고: https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/ 및 https://developer.apple.com/library/archive/documentation/AppleApplications/Reference/SafariWebContent/ConfiguringWebApplications/ConfiguringWebApplications.html

## 2026-09-14 Android maskable 아이콘
- manifest에 별도 `purpose: maskable` 항목 추가. 안전 영역을 충족하는 기존 `/app-icon-512.png`를 재사용하며 일반 `any` 항목 유지.
- 로고 픽셀의 최대 중심 거리 177.40px, 512px 아이콘의 최소 안전 원 반지름 204.8px 이내. 배경 불투명 확인.

## 2026-09-14 X 검토함 조회 부하 수정
- 운영 X 검토함 pending/visible API에서 1102 확인. 매 요청 전체 사진 쌍 비교 후 25건 절단하던 구조 제거.
- 사진 주소/해시/역방향 near/post별 인덱스로 후보 조회. 전체 글 pending 판정은 저장된 미확정 near 관계로 계산하며 탭 개수 및 필터 적용 전 판정 유지.
- 현재 페이지 25건에만 상세 비교 배열 생성. 후보 순서·중복 미디어·동일 그룹·비중복 판정·필터 밖 후보 메타데이터 보존.
- 전체 게시물 및 사진 메타데이터 읽기는 유지. 이번 수정은 전수 쌍 비교와 전체 상세 배열 생성 제거이며 SQL 자체의 완전한 페이지 조회 전환은 아님. DB/관리자 판정 쓰기 및 CPU 한도 변경 없음.
## 2026-09-15 URL 등록 사진·썸네일 자동 조회

사용자 승인: 입력창 하나에서 X·Instagram을 판별하고 사진/영상 썸네일을 가져온다. 직접 등록은 기존처럼 소유자가 선택한 자료로 취급하며, 이미 수집된 게시물은 검토 상태를 우회하지 않는다.

- [x] X FxEmbed 개별 게시물 조회와 Instagram Apify 개별 URL 실행/결과 조회를 분리. 원본 파일과 전체 수집 응답은 저장하지 않는다.
- [x] D1 작업 상태·잠금·Apify 실행 번호 저장. 신규 등록은 waitUntil로 시작하고 기존 Cron이 중단된 작업 및 기존 등록 자료를 이어 처리한다.
- [x] 공급자 실패 시 링크와 기존 사진 유지, 실패 이유·30초 재시도 간격. Apify 시작 결과가 불명확하면 자동 재실행하지 않는다.
- [x] 사진·영상 썸네일을 기존 갤러리에 표시. 자료 관리에 20건 단위 상태 조회 및 재시도 제공. 목록 조회 자체는 수집을 실행하지 않는다.
- [x] 입력 ID/출처 CDN 검증, 원문 작성자/게시일 보완, 인스타 p/reel 코드 중복 처리. 검토함의 기존 판단은 수정하지 않는다.
- [x] 운영 DB에 0020을 적용하고 GitHub 자동 배포 후 기존 직접 등록 X 자료의 영상 썸네일 보완 및 실제 피드 표시 확인.

운영: MANUAL_MEDIA_ENABLED=true가 있어야 외부 조회를 시작한다. Instagram은 기존 APIFY_TOKEN으로 apify/instagram-scraper를 URL 한 건/resultsLimit=1, timeout=120초, maxTotalChargeUsd=0.05 조건으로 실행한다. 기존 APIFY_TASK_ID의 예약 설정/입력은 변경하지 않는다. 이미 완료된 사진 새로고침은 새 실행, 실패한 진행 조회는 저장된 실행 번호를 재사용한다. 공급자 계약과 실제 결과는 VALIDATION 최신 절 참고.

## 2026-09-15 직접 등록 목록 접기·지연 조회
- [x] 기본 접힘 상태의 details/summary, 펼칠 때만 관리자 목록 조회.
- [x] 5개씩 이전·다음으로 교체. 새 등록 시 첫 페이지 자동 펼침.
- [x] 접기·탭 이동·모달 닫기에서 요청 취소 및 폴링 중지. 늦은 응답 무효화. 펼친 상태의 모달 재열기는 조회 재개.
- [x] 12건 표본으로 5/5/2 API 페이지와 브라우저 페이지 교체·접힌 동안 요청 없음 검증. 수집 작업 자체는 계속 진행.

## 2026-09-15 직접 등록 상태 새로고침 아이콘
- [x] 자료 관리의 상태 새로고침을 기존 수집 탭의 20px SVG/44px 버튼으로 통일. 접근성 이름과 title 유지.

## 2026-09-15 사진 새로고침 아이콘
- [x] 사진 조회 완료 항목의 새로고침을 상태 새로고침과 같은 아이콘으로 통일. 접근성 이름과 툴팁 유지.

## 2026-09-15 운영 관리 1단계 — 최소 운영 현황

사용자 승인 순서: 최소 운영 현황 → X 검토함 조회 구조 개선 → 증가 추이 기록 → 필요한 알림. 이번 변경은 첫 단계다.

- [x] 관리자 전용 /admin/operations 및 GET /api/admin/operations. 기존 인증 경계와 비공개 자산 규칙 재사용.
- [x] 확인할 항목 → X/Instagram 수집 → 직접 등록 사진 작업 → 저장 자료 규모 순서. 자료 관리에서 진입하고 기존 관리 화면으로 이동.
- [x] X 마지막 시도/성공/전체 동기화, 활성 여부, 다음 조회 가능 시각, 연속 실패/오류. 계정 목록 기본 접힘.
- [x] Instagram 외부 실행 확인과 자료 저장 시각 분리, 현재 설정 작업의 대기/오류 집계. 직접 등록 7개 상태별 건수.
- [x] COUNT 집계만으로 X/인스타/직접 등록 저장 건수 제공. 본문·전체 사진 비교를 조회하지 않음.
- [x] 수동 새로고침, 중복 요청 차단, 실패 시 이전 확인 시각/기록 표시, 인증 만료 시 내용 제거.

판정 기준: 기본 지연 유예 900초. X는 3분마다 1개 계정 처리 구조를 고려해 max(900, 활성·실행 가능 계정 수×180)초를 사용한다. next_due_at은 확정 실행 시각이 아닌 조회 가능 시각이다. 중지·유효한 처리 잠금·미래 재시도 예정은 지연으로 취급하지 않는다. Instagram의 자료 없는 정상 확인을 실패로 보지 않는다. 인스타 대기 지연은 동기화 스케줄러 정체 지표이며 개별 작업의 최초 대기 기간을 측정하지 않는다.

자료 규모는 저장 테이블별 건수로 숨김/검토 대기를 포함하며 고유 게시물 합계를 만들지 않는다. API는 SELECT만 사용하고 외부 호출·수집 실행·설정 변경·마이그레이션을 하지 않는다. 전체 DB 사용량/비용·일별 추이·알림·X 검토함 SQL 페이징은 이후 단계다.

- [x] 2단계: X 검토함의 전체 메타데이터 읽기를 페이지/비교 대상 조회로 전환.
- [x] 3단계: 자료 증가와 조회 부담의 추이를 기록.
- [x] 4단계: 지속되는 문제/복구 알림.

## 2026-09-15 운영 현황 UI 정리

사용자 요청: UI 감사의 부분 조회 실패 안내를 수정하고, 긴 스크롤과 흩어진 정보를 탭으로 정리한다.

- [x] 요약 / 수집·사진 / 자료 증가 / 알림 4개 탭. 요약은 현재 상태와 확인할 문제, 자료 증가는 현재 규모와 날짜별 기록을 한곳에 모음. 설명 기준은 접기로 정리.
- [x] 탭·패널 ARIA 연결, 방향키/Home/End 및 단일 탭 정지점. 선택한 탭 hash 유지, 기존 #growth-title 링크도 해당 탭으로 연결.
- [x] 요약의 문제와 알림 항목을 관련 탭·제목으로 연결하고 포커스 이동. 각 상세 화면의 기존 관리 페이지 진입 유지.
- [x] 부분 실패를 상단 role=alert로 항목별 안내하고 전체 성공 메시지 제거. 복구 시 오류 제거. API 전체 실패는 이전 기록과 선택 탭 유지, 인증 만료 시 모든 패널 제거.
- [x] 키보드·모바일·부분 실패/복구·기존 관리 흐름 검증, 코드 리뷰 및 자동 배포 확인.

조회는 기존 API의 한 응답을 모든 탭에서 공유한다. 탭 전환 시 추가 요청·폴링·수집·DB 쓰기는 하지 않는다. 기존 API/알림 판정/일별 기록은 변경하지 않는다.

## 2026-09-15 운영 관리 4단계 — 지속 문제·복구 알림

사용자 승인 범위: 1~3단계 뒤에 지속되는 운영 문제와 복구를 알린다. 알림 경로 질문은 답변 미수신으로 운영 현황 화면 안의 알림함을 기본 적용한다. 외부 메시지 전송·연결 설정은 포함하지 않는다.

- [x] 0023: 점검 제어1행, 고정 항목별 상태, 발생/복구/중지 이벤트. 본문·사진주소·공급자 원문 오류를 저장하지 않음.
- [x] 전용 5분 Cron. 기존 상태 판정을 공유하고 전체 게시물 건수/비교 조회는 생략. X 계정별, Instagram, 직접 등록 사진, 일별 기록 상태 확인.
- [x] 관찰된 문제가 최소15분 지속할 때 발생1회, 정상 관찰이 최소5분 간격으로2회 확인되면 복구1회. 동일 문제 반복 이벤트 없음. 수집 중지·완료는 중지로 종료, 정상 복구와 구분.
- [x] 처리 중/알 수 없음·점검 실패·15분 초과 관찰 공백은 후보/복구 확인의 연속성을 끊음. 이미 열린 문제는 정상 확인 전까지 유지. Instagram의 남은 오류/지연은 처리 중에도 문제로 유지.
- [x] DB 시간 기준120초 lease와 token, batch 전후 소유권 검사. 상태/이벤트/성공시각은 한 트랜잭션. 실패·만료된 실행은 성공시각을 앞당기거나 다음 실행의 잠금을 풀지 않음.
- [x] 조회는 저장값만 읽는 기존 소유자 API. 진행 중 문제 및 최근20개 이벤트, 점검 지연·첫 확인 대기·저장소 장애 구분. 기본 접힘 기록 및 내부 스크롤.
- [x] 단위·인증·회귀, 모바일/데스크톱, 실제 workerd/D1 검증·읽기 전용 코드 리뷰 후 0023 선적용과 자동 배포 확인. Cron 등록 확인, 첫 자동 실행은 대기 상태.

일별 기록: 매일00:05 예정에2시간 유예를 더해02:05부터 누락을 문제로 관찰한다. 당일 조회 측정 실패는 즉시 문제 관찰 대상이며 동일15분 확인을 거친다. 유예 중 전날 성공은 정상, 전날 실패는 확인 불가로 처리해 날짜가 바뀌었다고 복구하지 않는다. 임의 DB 용량/조회시간 임계값은 만들지 않는다.

운영 한계: 화면 안의 알림이므로 화면을 열지 않았을 때 외부로 통지하지 않는다. DB/Worker 전체 장애 중에는 내부 점검도 수행할 수 없고, 화면의 마지막 성공 점검시각으로 공백을 구분한다. 최근20건만 조회하지만 이력은 삭제하지 않는다. 재발 후 다시15분 지속하면 새 발생을 기록한다. 알림 조회·새로고침은 점검이나 외부 수집을 실행하지 않는다.

## 2026-09-15 운영 관리 3단계 — 자료 증가 추이

승인 범위: 기존 운영 현황에서 자료 증가와 대표 조회 부담을 날짜별로 확인한다. 기존 화면을 확장하는 작업으로, 사용자 승인 순서에 따라 구현한다.

- [x] 0022 집계 이력 테이블. 한국시간 하루 한 행, X/인스타/직접 등록 건수와 DB 크기, 대표 조회 SQL 시간·읽은 행·응답 크기만 저장.
- [x] 전용 Cron 매일 한국시간 00:05. 외부 수집과 분리하며 같은 날짜 재실행은 기존 기록 유지. 조회 측정 실패는 건수와 실패 상태를 저장하고, 기록 저장 실패는 성공으로 남기지 않음.
- [x] 대표 조회는 X 검토함 전체 탭 첫 25건+관련 후보. 실제 사용자 요청/방문자 정보·필터·본문·사진 주소는 저장하지 않음. 일반 조회에는 측정용 직렬화 추가 없음.
- [x] 운영 현황 GET은 저장 기록만 읽음. 최근 30일 및 전일 증감, 전날 기록 없을 때 비교 없음, 알 수 없는 측정값은 null. 이력 조회 장애가 현재 운영 현황을 가리지 않음.
- [x] 최신 요약과 기본 접힘 날짜별 표, 표 내부 스크롤. 빈 기록·측정 실패·오늘 기록 없음·인증 만료 및 모바일 검증.
- [x] 실제 D1 런타임, 회귀 테스트, 읽기 전용 검토 후 0022 선적용·GitHub 자동 배포 확인. 예약 설정 및 첫 운영 기준값 저장 확인; 첫 자동 예약 실행은 별도 관찰 대상.

범위/판단: 일별 최초 저장값을 유지하므로 하루의 총 유입량이 아닌 두 측정 시점의 순증감이다. 이력은 연간 약365개의 작은 집계 행으로 보관하며 원본·이력 삭제는 하지 않는다. 조회 비용은 대표 표본 한 번의 값으로 전체 서비스 사용량/요금이나 사용자 체감 응답시간이 아니다. 응답 크기는 JSON 본문 UTF-8 바이트이며 DB 반환량/HTTP 헤더를 포함하지 않는다. DB 크기는 집계 SELECT의 D1 size_after이며 이미지 원본 파일 크기가 아니다. 측정 실패·예약 누락 시 빈 날짜를 보완하거나 성공값을 만들어내지 않는다. 향후 쿼리 의미 변경 시 query_version도 변경한다. 알림은 4단계다.

## 2026-09-15 운영 관리 2단계 — X 검토함 DB 페이지 조회

승인 범위: 분류·필터·정렬·개수 및 페이지 밖 비교 후보를 유지하면서 현재 25개 게시물의 본문만 조회한다. 화면/판정 API는 유지한다.

- [x] 0021: 작은 게시물 메타데이터/사진 참조 색인 생성 및 기존 posts에서 보완. INSERT/UPDATE/DELETE 트리거로 원본과 같은 트랜잭션에서 갱신. 기존 원본/판정/커서 수정 없음.
- [x] 단일 SQL에서 공통 분류·개수·페이지·비교를 함께 계산. 조회 가능 사진의 대표 게시물은 같은 해시의 앞선 적격 게시물 존재 여부로 판정.
- [x] 현재 페이지의 해시/정방향·역방향 near 관계를 색인으로 조회. 필터 밖 후보, 반복 미디어, 제외 판정, confirmed_hash, 비교 대상 revision 유지.
- [x] 결과는 메타/작성자/본문/비교별 개별 행 반환. 대규모 비교를 단일 JSON 셀로 집계하지 않음.
- [x] 기존 GET 보존본과 288개 필터·페이지 조합 대조, 상태 변경·null·정렬·rollback·800건 동일 그룹 검증.
- [x] 실제 로컬 workerd/D1에서 3천/6천 건의 응답·DB 읽기·전송량 비교 및 마이그레이션 전후 기존 조회 일치 확인.
- [x] 운영 0021 선적용 → 파생 행/원본 보존 확인 → GitHub 자동 배포 → 운영 조회 검증. 실제 소유자 브라우저 확인은 미검증이며 운영 D1 대조와 구분한다.

비용/범위: 정확한 개수·전역 분류에는 전체의 작은 색인 행 조회가 남는다. 6천 건 표본의 DB 읽기는 84,003→96,511(+약15%)이고 본문 전송·Worker 처리량은 크게 감소한다. 새 색인 저장 및 수집 시 쓰기 비용이 추가된다. 실제로 관련된 비교 후보가 매우 많은 경우 응답 크기는 계속 커질 수 있다. 후보 잘라내기는 이번 동작 보존 범위에서 하지 않는다. 다음 3단계에서 증가 추이를 관찰한다.

배포 순서: additive 0021을 먼저 적용해 이전 코드와 호환성을 유지한다. 과거 이력 누락 파일을 일괄 재실행하지 않는다. 코드 롤백 시 원본/판정은 보존하며 새 파생 색인은 남겨도 이전 코드와 호환된다.

## 2026-09-15 운영 현황 이동 링크 아이콘

- [x] 운영 현황 범위의 피드/로그인/수집/자료/인스타/검토 내역 및 탭 내부 이동 링크를 아이콘으로 통일. 문제 설명과 건수, 탭 이름·로고·건너뛰기는 텍스트 유지.
- [x] 접근성 이름, 키보드 툴팁, 44px 대상, 모바일 넘침 및 기존 화면 이동 검증.
- [x] 두 독립 디자인 평가 후 결과 기록 및 기존 비공개 환경 자동 배포 확인.

## 2026-09-15 운영 현황 정보 우선순위 정리

- [x] 사용자가 승인한 세 항목: 반복 문구 축소, 확인할 문제를 요약 첫 구역으로 이동, 상세 지표 접기.
- [x] 성공 안내 role=status 유지·시각적 중복 제거, 로딩은 표시. 문제가 없는 안내/부분 실패/이전 기록/인증 만료 안내 유지.
- [x] Instagram 오류·대기는 기본 표시, 확인·저장 시각은 접기. 직접 등록 실패·대기·진행은 기본 표시, 완료·기타3개는 접기. 수치 누락 없음, 펼침 상태 새로고침 유지.
- [x] 브라우저 회귀·4개 너비·키보드 및 읽기 전용 코드 리뷰.
- [x] 기존 비공개 환경 자동 배포 확인.

## 2026-09-17 최신 수집 상태와 과거 범위 안내 분리

- [x] 운영 현황과 수집 관리 표시를 대조. 과거 범위 미확인/분량 제한은 현재 수집 오류와 분리하고 별도 건수·설명으로 표시.
- [x] 실제 실패·지연·needs_attention, 중지 상태와 기존 알림 복구 확인 절차 유지. 수집 커서/일정/원본/과거 알림 기록 변경 없음.
- [x] 회귀 테스트, 화면 4개 너비, 독립 읽기 전용 리뷰 및 로컬 번들 검증.
- [x] GitHub 자동 배포와 운영 화면 확인.

