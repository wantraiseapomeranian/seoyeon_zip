# 서연모음.zip 작업 지침

## 기본 역할과 범위
- 이 프로젝트의 기본 역할은 구현과 검증을 수행하는 개발자다.
- 사용자가 이 프로젝트에 대해 요청한 예외에 따라, 개인 지침의 리뷰 전용 역할과 그에 따른 일괄 파일 수정·테스트·빌드 금지는 기본 개발 작업에 적용하지 않는다. 그 밖의 개인 선호는 유지한다.
- 요청한 작업에 필요한 코드·문서 수정, 도구 설치, 관련 테스트와 빌드를 수행한다. 불필요한 전체 탐색과 반복 검증은 피한다.
- 기존에 승인한 seoyeon-zip Cloudflare 비공개 검증 환경의 배포는 작업 범위에 포함한다. Access 보호를 유지한다.
- 공개 전환, 결제, 데이터 삭제는 별도 사용자 요청 없이 수행하지 않는다.
- 사용자가 명시적으로 리뷰만 요청하면 해당 작업은 읽기 전용으로 진행하고 Critical/Major 문제만 간결하게 보고한다.
- 최신 사용자 지시와 실행 환경의 권한 제한을 따른다.

## GitHub 버전 관리와 배포
- 기본 작업 흐름은 수정 → 관련 검증 → 개인정보·비밀값 및 변경 diff 점검 → 커밋·푸시 → Cloudflare 자동 배포 결과 확인이다.
- 사용자가 요청한 구현 작업에는 해당 변경의 커밋·푸시와 기존 비공개 환경의 자동 배포 확인을 포함한다. 매번 별도 승인을 요청하지 않는다. 사용자가 리뷰만 요청하거나 커밋·푸시를 금지한 작업은 제외한다.
- GitHub 저장소 wantraiseapomeranian/seoyeon_zip을 코드 이력의 기준으로 유지한다. 작업 시작 시 현재 브랜치와 원격 변경을 확인하고, 작업 단위로 의미 있는 커밋을 남긴다. 무관한 변경은 임의로 포함하지 않는다.
- 공개 저장소에 개인정보, 토큰, 비밀값, 로컬 수집 원본, 인증 상태 파일이 포함되지 않도록 커밋 전에 점검한다. 확인되지 않은 파일을 일괄 추가하지 않는다.
- Cloudflare와 연결된 배포 브랜치를 확인하고 기존 브랜치 보호·PR 절차를 따른다. 강제 푸시나 보호 규칙 우회는 하지 않는다.
- 코드 배포는 GitHub 연동 자동 배포를 기본으로 한다. 직접 Wrangler 배포가 필요한 예외는 이유를 설명하고, 대응 커밋과 배포 버전을 기록해 GitHub와 배포 코드가 다른 상태로 남지 않게 한다.
- DB 마이그레이션은 코드 호환성을 확인해 필요한 순서로 적용한다. 기존 Access 보호를 유지한다.
- 푸시 성공과 배포 성공을 구분한다. 배포된 커밋과 결과를 확인하고, 실패·대기·미확인 상태는 그대로 보고한다.

## 프로젝트 기준
- 디자인은 최신 DESIGN.md를 우선하고 기능 요구사항은 docs/SPEC.md를 참고한다.
- 계획은 docs/PLAN.md, 실제 검증 결과와 미확인 사항은 docs/VALIDATION.md에 기록한다.
- 수집 응답 전체의 처리가 끝나고 저장이 성공한 뒤에만 cursor를 전진한다.
- 실제 작성자와 발견 소스를 분리한다. 이름 없는 미디어 예외는 검증된 전용 계정의 직접 작성 글에만 적용하고 재게시·인용은 별도 판별한다.
- 로컬 성공과 Cloudflare 운영 검증을 구분한다. 실패·미확인 결과를 성공으로 보고하지 않는다.
